# 测试/测开 笔试题目小结

**1. 关于采用探索性测试原因，最合适：**

探索性测试可以发现一些正式测试中遗漏的缺陷

**2. ATM取款功能，取款数只能输入正整数，每次取款数要求是100的倍数且不能大于500，正确的无效等价类：**

（500，+∞）、任意大于0小于500的非100倍数的整数

**3. 系统并发访问数估算数据最有效：**

高峰时段平均每秒请求数80（√）

同时在线用户数100、高峰时段日处理业务量100000、平均每秒用户请求数50（❌）

**4. 进程**

可同时执行的进程是指若干进程同时占用处理器

**5. “均匀分散、齐整可比”这个描述与什么测试方法一般知识有关**

正交法

**6. 有效明确的功能需求：**

登录后显示公告栏（√）

两年内存储数据量不超过100G、界面美观大方、长时间操作后提醒用户休息（❌）

**7. 系统测试阶段的测试对象**

硬件系统、软件系统、文档（√）

源程序（❌）

**8. 使用瀑布模型术语，在软件测试V模型中，对应“需求分析”的测试阶段是**

系统测试

对应：项目计划——验收测试、需求分析——系统测试、软件设计——集成测试、程序开发——单元测试

**9. 如何减少换页错误**

访问局部性满足进程要求

进程倾向于占用I/O（而不是CPU）

**10. 以测试的形态分测试可以分为：**

建构性测试、系统测试、专项测试

**11. 同步机制应该遵循哪些基本准则**

空闲让进、忙则等待、有限等待、让权等待

**12. 1<=n<100的边界值：**

1、100、99、101

**13. 假设有个空文件file，执行如下命令：**

```shell
echo hello >> file;
echo world > file;
cat file
```

输出为world

使用>>指令向文件追加内容，原内容将保存

使用>指令覆盖文件原内容并重新输入内容，若文件不存在则创建文件

**14. 关于软件测试的描述：**

测试过程中某模块中发现的bug越多，该模块残留的bug越多（❌）

**15. 代码走查目的：**

发现缺陷、遗漏和矛盾的地方

**16. Java调试器，如果编译器返回程序代码的错误，可以用它对程序进行调试**

jdb.exe

java.exe:java虚拟机

javadoc.exe:制作java文档

javaprof.exe:剖析工具

**17. MAC地址**

一台计算机只能有一个MAC地址（❌）

**18. 瀑布模型表达了一种系统的、顺序的软件开发方法，关于瀑布模型：**

瀑布模型适用于软件需求确定、开发过程能够采用线性方式完成的项目

**19. 正在执行的进程，由于期待的某些事件未发生，则由系统自动执行阻塞原语（Block），使自己由运行状态变为阻塞状态。进程阻塞原因不包括：**

不包括：时间片切换（**时间片切换是由运行态切换到就绪态**）

包括：等待I/O、进程sleep、等待解锁

**20. I/O系统硬件结构分为四级：**

由高到低：处理机——I/O通道——设备控制器——I/O设备

**21. 数组比线性表操作速度更快：**

原地逆序、返回中间节点

**22. 移动硬盘盒U盘是两种常见移动存储设备，关于移动硬盘盒U盘**

二者都呈现单位存储的价格越来越低的趋势

二者都可以通过USB总线盒计算机连接

**23. fork()**

```c
#include <stdio.h>
int main(){
	fork();
	fork();
	fork();
	printf("hello\n");
	return 0;
}
```

执行后返回8个hello

**fork()调用一次返回两次，2^3=8**

**24. Linux命令中可以显示文本文件内容：**

less、tail、cat

**25. Linux进程内存空间**

从高地址到低地址：stack、heap、bss segment、data segment、code segment/text segment

**26. 假设TTL初始值为3，两台主机之间存在4个路由器，则数据包到达目的地后TTL值变为：**

数据报无法到达

**数据报每经过一台路由器时，路由器将TTL值减1**

**27. 一个bash shell脚本上的第一行通常加上哪一句：**

#!/bin/sh

**28. 对于需求文档的测试，在哪一个阶段进行**

系统测试

**29. 同一个进程下的线程可以共享什么**

**共享**：进程代码段、进程数据段、进程打开的文件描述符

**私有**：线程ID、寄存器组的值、错误返回码、线程堆栈

**30. java内存区**

JVM中的堆区和栈区是私有的，不能被线程共享（❌）

**31. 元素比较次数与元素初始排列无关的排序算法**

选择排序

**32. 关于归并排序**

在实际中，归并排序比快速排序要快，因为它的比较次数少，所以大部分排序算法底层是归并排序（❌）

**33. 关于内存管理方式**

段页式管理是段式管理与页式管理方案结合而成，有两者的优点，使执行速度提升（❌）

**段页式管理缺点是执行速度下降**

**34. TCP协议**

TCP在视频传输方面取代了UDP功能（❌）

TCP建立连接需要3次握手，3次握手也可能对其中造成被flood攻击的可能（√）

**35. 微信公众号相关的H5页面测试时**

手机设置里定位未被打开时，H5页面定位将要手动选择（❌）

公众号未授权进行定位时，H5页面定位将要手动选择（√）

**36. 将sh test.sh命令在后台执行，并且保证当前终端断开连接情况下命令仍然执行：**

screen打开后台终端，执行sh test.sh

**37. 度为m的哈夫曼树中，叶子节点个数为n，非叶子节点个数为：**

（n-1）/（m-1）

设非叶子节点数为x，总节点个数为x+n，度为m哈夫曼树，每个非叶子节点都有m个分叉，因此节点总数为x*m+1（加上根节点），x+n=x * m+1

**38. linux线程**

线程自己拥有很少资源，但它可以使用所属进程的资源

由于同一进程中多个线程具有相同地址空间，所以它们间的同步和通信也易于实现

进程创建与线程创建的时空开销不相同

**⭐进程是资源分配基本单位，线程是资源调度基本单位**

**39. 哪些状态为TCP连接关闭过程中出现**

TIME-WAIT、LAST-ACK

**40. 和192.168.3.110/27属于同一个子网**

192.168.3.124、192.168.3.126

⭐192.168.3.96不算（**主机号全1为子网广播地址，全0为子网网络地址**）

**41. 若一序列进栈顺序a1、a2、a3、a4，存在多少种出栈序利**

14

**卡特兰数公式**

**42. 基本路径法测试**

覆盖路径数=判定条件语句节点个数+1

**43. Linux系统与Windows系统进程间通信和线程间通信**

Linux进程间通信：管道、信号、消息队列、共享内存、信号量、套接字

Linux线程间通信：互斥量、信号量、条件变量

Windows进程间通信：管道、消息队列、共享内存、信号量、套接字

Windows线程间通信：互斥量、信号量、临界区、事件

**44. ^d+[ ^ d]+能匹配的字符串**

d123

^d：头是d

+：前面的d出现1-n次

{^d]+：匹配除了d之外任何字符

**45. Python代码**

```python
foo = [1,2]
foo1 = foo
foo.append(3)
```

foo和foo1都指向同一个地址，它们的值都是[1,2,3]

**46. C++拷贝构造函数在什么情况下被调用**

使用一个类的对象去初始化该类的一个新对象

被调用函数的形参是类的对象

当函数返回值是类的对象时，函数执行完成返回调用者

**47. 随机测试**

随机测试是根据测试的经验在无测试用例情况下进行的测试

**48. 将当前目录下所有pdf文件内容转换成txt文件内容**

\# shell脚本编程 

```shell
  for f in `ls *.pdf`  # 倒引号表示shell执行这条语句，并将执行结果作为这个表达式的值 

  do 

    pdftotext 

    “$f”          # shell在碰到“$”的变量时会自动将其替换为这个变量的值 

  done
```

**49. 如何从有数字规律的网址抓取网页并保存在当前目录，假设网址为http://test/0.xml，其中这个数字可以递增到100**

```shell
for(i=0;i<100;++i) 
do 
    wget http://test/$i.xml 
done
```

**50. Linux系统中查找可执行文件的命令：**

which：查看可执行文件的位置

whereis：查看文件的位置

locate：配合数据库查看文件位置

find：实际搜寻硬盘查询文件名称

type：区分某个命令是由shell自带还是由shell外部的独立二进制文件提供

**51. 假设某分时系统采用时间片轮转发法，进程A，B，C，D需要运行的时间分别为20ms、10ms、15ms、5ms，时间片选用5ms，平均周转时间为**

进程运行顺序为：A B C D A B C A C A

(10+6+9+4)*5/4=36.25ms

**52. 进程和线程**

线程也拥有自己的系统资源（❌）

线程之间不共享进程中的共享变量和部分环境（❌）

**53. 死锁**

银行家算法用在预防死锁策略中（❌）

**银行家算法通过控制资源分配避免死锁**

**54. 用1*3瓷砖密铺3 * 20地板有几种方式**

a(i)=a(i-1)+a(i-3)

a(1)=1、a(2)=1、a(3)=2。求解a(20)

**55. 基于哈希算法在信息安全中主要应用在**

文件校验、数字签名、鉴权协议

**56. 截取logfile文件中包含suc的行，并只输出最后一列：**

grep 'suc' logfile | awk '{print $NF}'

**57. 单元测试的测试用例主要根据（）的结果来设计**

详细设计

**58. 系统测试使用哪种技术**

黑盒测试

**59. 黑盒测试、白盒测试、灰盒测试**

灰盒测试：灰盒分析，基于对程序内部细节有限认知上的软件调试方法

**60. 负载测试和压力测试**

系统的最高能力是压力测试

超负荷情况是负载测试

**61. 系统测试的策略**

系统测试的16个策略：功能测试，性能测试，压力测试，容量测试，安全性测试，GUI测试，可用性测试，安装测试，配置测试，异常测试，备份测试，健壮性测试，文档测试，在线帮助测试，网络测试，稳定性测试 

**62. TDD测试驱动开发**

测试驱动开发是敏捷开发中的一项核心实践和技术，也是一种设计方法论。TDD得原理是在开发功能代码之前，先编写单元测试用例代码，测试代码确定需要编写什么产品代码。TDD虽是敏捷方法的核心实践，但不只适用于XP（Extreme Programming），同样可以适用于敏感词开发方法和过程。TDD得基本思路就是通过测试来推动整个开发得进行，但测试驱动开发并不只是单纯的测试工作，而是把需求分析，设计，质量控制量化的过程。TDD的重要目的不仅仅是测试软件，测试工作保证代码质量仅仅是其中一部分，而且是在开发过程中帮助客户和程序员去除模棱两可的需求。TDD首先考虑使用需求（对象、功能、过程、接口等），主要是编写测试用例框架对功能的过程和接口进行设计，而测试框架可以持续进行验证。 
 优点：在任意一个开发节点都可以拿出一个可以使用，含少量bug并具一定功能的产品。 
 缺点：增加代码量。测试代码是系统代码的两倍或更多。

**63. 涉及到路径测试：**

单元测试、集成测试

（系统测试和验收测试都是功能测试）

**64. 设计系统测试计划需要参考的项目文档**

软件测试计划、软件需求规范、迭代计划

可行性研究报告（❌）

**65. 测试方法分成哪几种**

个人复查、抽查和会审、黑盒测试、白盒测试

**66. 属于动态分析：**

模块功能检查、系统压力测试

**67. Junit单元测试：**

@Test注解标注的测试方法只能说public void的，且不能有任何输入参数

JUnit底层实现上，用System.exit退出用例执行

**68. 测试设计员的职责**

设计测试用例，设计测试过程、脚本

**69. 性能测试**

系统性能测试中应测试的关键是：交易执行响应时间指标

**性能和时间挂钩**

**70. 研发需要对自己的代码审核，检查代码的正确性，这个过程称为：**

代码走查

**71. 系统测试用例的设计应从什么时候开始**

需求完成

**72. 测试工具**

JMeter：基于JAVA的压力测试工具，Badboy用来进行脚本录制

Junit：白盒测试工具，针对代码测试

LoadRunner：负载压力测试

TestLink：用例管理工具

**73. 集成测试的过程**

构建的确认过程、系统集成测试组提交过程、测试用例设计过程、Bug的报告过程

**74. 对手机软件的压力测试通常包括**

存储压力、响应能力压力、网络流量压力

**75。 单元测试工具**

PureCoverage、Purify、Quantify

**76. 集成测试计划**

集成测试计划在需求分析阶段末提交（❌ ）

集成测试计划在概要设计之后

**77. 单元测试的策略**

逻辑覆盖、循环覆盖、同行评审、桌前检查、代码走查、代码评审、景泰数据流分析

**78. 软件测试计划评审会需要哪些人员参加**

项目经理、SQA负责日、配置负责人、测试组

**79. 单元测试主要技术手段**

驱动代码、Stub代码、Mock代码

**80. 哪种测试模型把测试过程作为需求分析、概要设计、详细设计及编码之后的阶段**

V模型

**81. 系统测试计划**

系统测试计划属于项目阶段性关键文档，需要同行评审（√）

**82. 系统测试的内容**

压力测试、功能测试、安全测试、性能测试

**接口测试属于集成测试**

**83. 侧重于观察资源耗尽情况下软件表现的系统测试为**

压力测试

**84. 网络管理员编写了shell程序prog1.sh，测试时程序死循环无法结束，通过什么方式结束程序**

ctrl+C

**85. 既可以用于黑盒测试，也可以用于白盒测试方法**

边界值法

**86. 桩（stub）函数**

在单元测试中被其他模块调用

在自顶向下集成过程中尤其有效

**87. 软件测试类型按开发阶段划分：**

单元测试、集成测试、确认测试、系统测试、验收测试

**88. 使用软件测试工具的目的包括**

帮助测试寻找问题、协助问题的诊断、节省测试时间

提高设计质量（❌）

**89. alpha测试**

alpha测试需要用户参加、alpha测试是验收测试一种

**90. iOS单元测试框架**

OCUnit、GHUnit、OCMock

**91. 负载测试、容量测试、强度测试**

负载测试：在一定的工作负荷下，系统的负荷及响应时间。 

强度测试：在一定的负荷条件下，在较长时间跨度内的系统连续运行给系统性能所造成的影响。 

容量测试：容量测试目的是通过测试预先分析出反映软件系统应用特征的某项指标的极限值（如最大并发用户数、数据库记录数等），系统在其极限值状态下没有出现任何软件故障或还能保持主要功能正常运行。容量测试还将确定测试对象在给定时间内能够持续处理的最大负载或工作量。容量测试的目的是使系统承受超额的数据容量来发现它是否能够正确处理。容量测试是面向数据的，并且它的目的是显示系统可以处理目标内确定的数据容量。